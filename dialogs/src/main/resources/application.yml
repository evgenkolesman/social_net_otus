spring:
  application:
    name: dialogs
  flyway:
    default-schema: ${db.schema}
    locations: "classpath:db/migrations"
  devtools:
    add-properties: false

app:
  sharding:
    total-shards: 32
    default-shard: 0
